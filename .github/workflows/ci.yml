name: CI

on:
  push:

permissions:
  id-token: write
  contents: write
  actions: read
  checks: read
  deployments: write
  artifact-metadata: write

jobs:
  build:
    uses: wow-look-at-my/go-toolchain/.github/workflows/build.yml@v1
    secrets: inherit
